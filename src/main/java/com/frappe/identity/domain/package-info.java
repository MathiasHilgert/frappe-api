/**
 * Identity's domain: value objects ({@link com.frappe.identity.domain.Password},
 * {@link com.frappe.identity.domain.EmailAddress}), the one {@link com.frappe.identity.domain.PasswordPolicy} for every
 * password identity stores, and the ports its adapters implement. Pure Java.
 */
package com.frappe.identity.domain;
