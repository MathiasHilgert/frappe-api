/**
 * Platform kernel: the shared contracts other modules build on (event envelope, id generation, business metric
 * declarations, the {@code @CommandUseCase} / {@code @QueryUseCase} stereotypes with {@link com.frappe.platform.Result}
 * for expected failures).
 */
package com.frappe.platform;
