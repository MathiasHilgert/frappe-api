/**
 * Localization contracts every module builds on: the supported locales, the hooks through which identity and
 * organization contribute a user's and a tenant's locale, and the ICU-backed application {@code MessageSource} that
 * resolves user-facing text from per-module catalogs.
 */
@NamedInterface
package com.frappe.platform.i18n;

import org.springframework.modulith.NamedInterface;
