/**
 * Platform infrastructure: adapters for ids, persistence, the outbox, NATS and use cases. This package
 * holds only the notifications the adapter packages exchange without depending on each other.
 */
package com.frappe.platform.infrastructure;
