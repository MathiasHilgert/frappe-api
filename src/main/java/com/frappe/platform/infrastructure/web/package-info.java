/**
 * HTTP routes: the startup checks that give every route a declared posture, the {@code /v1} prefix, and the
 * enforcement of the posture before controller code runs.
 */
package com.frappe.platform.infrastructure.web;
